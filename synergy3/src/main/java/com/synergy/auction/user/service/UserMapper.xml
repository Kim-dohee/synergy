<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.synergy.auction.user.service.UserMapper">
	<insert id="userInsert"
			parameterType="com.synergy.auction.user.service.UserDto">
		INSERT INTO user(
			user_id
			,user_pw
			,user_name
			,user_phone
			,user_email
			,user_nick
			,user_joindate
		) VALUES (
			#{userId}
			,#{userPw}
			,#{userName}
			,#{userPhone}
			,#{userEmail}
			,#{userNick}
			,now()
		)
	</insert>
	
	<select id="userSelectOne" parameterType="String" resultType="com.synergy.auction.user.service.UserDto"> 
		SELECT
			user_no AS userNo
			,credit_right_no AS creditRightNo
			,user_id AS userId
			,user_pw AS userPw
			,user_name AS userName
			,user_phone AS userPhone
			,user_email AS userEmail
			,user_nick AS userNick
			,user_joindate AS userJoindate
		FROM 
			user 
		WHERE user_id=#{userId} 
	</select> 
	
	<insert id="creditRecordInsert"  
		parameterType="com.synergy.auction.user.service.CreditRecordDto">  
		INSERT INTO credit_record(  
			user_no
			,credit_record_now_score
		) VALUES (  
			#{userNo}  
			,80  
		)
	</insert>  
	<update id="userUpdate" parameterType="com.synergy.auction.user.service.UserDto"> 
		UPDATE user SET 
			user_pw = #{userPw} 
			,user_nick = #{userNick} 
			,user_phone = #{userPhone} 
			,user_email = #{userEmail} 
		WHERE user_no = #{userNo} 
	</update> 
	
	
</mapper>